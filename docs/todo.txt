
1. Change Handle<TextureResource> to use the ResourceHandle specialization that Handle<Audio> uses

2. Extend RigidBody serialization to capture the physics body's current linear/angular velocity, etc.

3. Add ctrack

4. Add integration between Error and std::errorcode/exceptions

5. Remove GameLoopSystem - UserSystem stuff has made producing events for phases unnecessary 

6. Make "isAwake" a field inside B2BodyParameters, erase default true inside RigidBodyComponentBuilder::Build

7. Figure out a good way to clear out user-registered components for a scene change

8. Reimplement SizedEnumMap using magic_enum

9. Replace Components inheriting from BaseComponent with a specialization of ComponentTraits to be able to use PFR with them

10. Ensure that erasing music/sounds from AudioBank doesn't make AudioSystem explode

11. Remove atlasId from SpriteInfo & FontInfo, just retrieve it from atlas texture using XInfo::atlasIndex